<li class="list-group-item bg-light">
  <div class="d-flex justify-content-between">
 
    <% if item.ready %>
      <span class='text-muted'>
        <%= item.description %>
      </span>
      <%= link_to unready_car_item_path(@car, item), method: :patch, class:'btn btn-info btn-sm'  do %>
          <i class='fa fa-check'></i>
      <% end %>
    <% else %>
      <span class='text-muted'>
        <%= item.description %>
      </span>
      <%= link_to ready_car_item_path(@car, item), method: :patch, class:'btn btn-dark btn-sm'  do %>
          <i class='fa fa-times'></i>
      <% end %>
    <% end %>
  </div>
</li>


